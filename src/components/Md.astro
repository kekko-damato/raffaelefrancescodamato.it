---
import { renderEm } from '../i18n/markers'

interface Props {
  text: string
  tag?: string
  class?: string
  id?: string
}
const { text, tag = 'span', class: className, id } = Astro.props
const html = renderEm(text)
const Tag = tag as keyof HTMLElementTagNameMap
---
<Tag class={className} id={id} set:html={html}></Tag>
